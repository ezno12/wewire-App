export type UserType = {
    id: number,
    firstname: string,
    lastname: string,
    email: string,
    password: string
}

export type LoginType = {
    email: string,
    password: string
}